{
  "name": "kestra-gitops",
  "version": "1.0.0",
  "description": "Kestra GitOps boilerplate with validation hooks",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "validate": "bash ./scripts/validate.sh"
  },
  "devDependencies": {
    "husky": "^9.0.11"
  }
}
